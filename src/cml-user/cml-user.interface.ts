export interface FilterTypeLOLRO {
  userName: string;
  userPosition: string;
  staffId: string;
  searcherUserId: string; // iuser_id: the one who is searching
  page: number;
  role_id: number;
  targetUserIdFilter: string; // iuser_id_filter: the one being searched
}
